{
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node scripts/copy-frontend.js && npm --prefix backend install",
    "start": "node backend/src/server.js"
  }
}